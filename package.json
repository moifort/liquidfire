{
  "name": "@moifort/fireway",
  "version": "1.0.5",
  "description": "Schema migration tool for Firestore",
  "main": "src/index.js",
  "license": "MIT",
  "bin": {
    "fireway": "src/cli.js"
  },
  "scripts": {
    "release": "git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm login && npm publish --access=public"
  },
  "dependencies": {
    "@firebase/testing": "0.20.10",
    "consola": "^2.15.0",
    "firebase-admin": "9.1.0",
    "md5": "2.3.0",
    "sade": "1.7.3"
  }
}
